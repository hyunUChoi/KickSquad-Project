<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.smhrd.mapper.BoardMapper">
    
    <select id="boardList" resultType="kr.smhrd.model.BoardVO">
        select * from board2
    </select>
    
    <insert id="boardInsert" parameterType="kr.smhrd.model.BoardVO">
       insert into board2(title, writer, contents, id)
       values(#{title}, #{writer}, #{contents}, #{id});       
    </insert>
    
    <select id="boardSearch" resultType="kr.smhrd.model.BoardVO">        
        select * from board2 where idx = #{idx}
    </select>

   
</mapper>